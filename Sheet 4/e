#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    int i,sum=0;
    cin>>s;
    for (i = 0; i < s.size (); i++)
    {
        sum+=s[i]-'0'; //0+3(ascii 51) - 48
    }
    cout<<sum;
}
